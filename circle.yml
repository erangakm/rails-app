deployment:
  staging:
    branch: master
    commands:
      # appname: desolate-escarpment-18459
      - git push git@heroku.com:desolate-escarpment-18459.git $CIRCLE_SHA1:refs/heads/master
      - heroku run rails db:migrate --app desolate-escarpment-18459
